#include <stdio.h>

int main() {
    char hello world;
    
    // Take a single word as input
    printf("Enter a word:%s\n ",hello);
    scanf("%s", hello);
    
    // Print the word using printf
    printf("You entered: %s\n", world);
    
    return 0;
}
