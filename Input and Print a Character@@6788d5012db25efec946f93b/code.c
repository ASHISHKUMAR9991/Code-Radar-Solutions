#include <stdio.h>

int main() {
    char ch;

    // Taking a single character input using scanf
    printf("Enter a character: ");
    scanf("%c", &ch);
    printf("You entered: %c\n", ch);

    return 0;
}
